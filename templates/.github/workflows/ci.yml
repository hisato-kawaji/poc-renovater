name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Secret Scan
        run: echo "Running secret scan..." # Mock for trufflehog
      - name: Build & Test
        run: echo "Running build and test..."
